# Auto-generated from production-secrets.yaml
# Document 3: SecretStore

# External Secret Store Configuration
apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: emotiond-secret-store
  namespace: emotiond
spec:
  provider:
    aws:
      service: SecretsManager
      region: us-west-2
      auth:
        jwt:
          serviceAccountRef:
            name: emotiond-api-sa
