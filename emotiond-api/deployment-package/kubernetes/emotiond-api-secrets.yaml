# Auto-generated from production-secrets.yaml
# Document 1: Secret

# Production Secrets Configuration
# IMPORTANT: This is a template. Actual secrets should be managed securely.
# Use external secret management (AWS Secrets Manager, HashiCorp Vault, etc.)

apiVersion: v1
kind: Secret
metadata:
  name: emotiond-api-secrets
  namespace: emotiond
  labels:
    app: emotiond-api
    environment: production
type: Opaque
data:
  # Base64 encoded values
  # Generate with: echo -n "your-secret" | base64
  jwt-secret: <BASE64_ENCODED_JWT_SECRET>
  database-url: <BASE64_ENCODED_DATABASE_URL>
  redis-url: <BASE64_ENCODED_REDIS_URL>
  api-keys: <BASE64_ENCODED_API_KEYS>
  webhook-secret: <BASE64_ENCODED_WEBHOOK_SECRET>
  encryption-key: <BASE64_ENCODED_ENCRYPTION_KEY>
