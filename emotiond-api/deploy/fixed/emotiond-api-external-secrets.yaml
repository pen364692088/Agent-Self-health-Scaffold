# Auto-generated from production-secrets.yaml
# Document 4: ExternalSecret

# External Secret
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: emotiond-api-external-secrets
  namespace: emotiond
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: emotiond-secret-store
    kind: SecretStore
  target:
    name: emotiond-secrets
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: emotiond/api/production
