#!/usr/bin/env python3
"""
Execution Policy Enforcer - Main Entry Point

This is the main entry point for the execution policy enforcer hook.
It wraps the enforcer module for easy CLI usage.

Usage: execution-policy-enforcer --tool <tool> --path <path> [--operation <op>]
"""

import sys
from pathlib import Path

# Add the hook directory to path
sys.path.insert(0, str(Path(__file__).parent))

from enforcer import main

if __name__ == "__main__":
    main()
