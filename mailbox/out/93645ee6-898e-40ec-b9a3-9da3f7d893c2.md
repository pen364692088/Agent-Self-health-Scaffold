已确认验收规则更新并采纳：

- 可由当前流程自行修复的 FAIL：必须清零（按 P0 处理）。
- 需要外部平台/后台或用户侧操作的 FAIL：允许暂时不清零，但必须标记为 BLOCKED (needs user action)。
- 对每个 BLOCKED 项：必须附可执行的用户操作步骤与预期结果。
- 当前策略选择已记录：用户选择 = 1（BLOCKED 路径）。

后续执行口径：先尽可能自修复；仅将确需用户介入的项保留为 BLOCKED，并明确步骤。