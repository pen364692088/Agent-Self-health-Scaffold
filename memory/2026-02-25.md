# 2026-02-25 Session Log

## Summary
- Delivered the queued daily briefing to user in normal voice (no system metadata leakage).
- Implemented daily briefing freshness upgrade in `daily-briefing/fetch-rss-v1.js` and committed locally.
- Added RSS recency filtering (default 48h), fallback queries, and clearer empty-data behavior per category.
- Audited cron jobs and diagnosed repeated `Manager healthcheck` failures.
- Fixed healthcheck cron model routing by forcing full provider-qualified model (`ollama/qwen2.5:3b-instruct`) and validating with manual run.

## Decisions
- Keep `daily-briefing-7am` active; improve data quality via source-side filtering instead of relying on model “联网能力”.
- For cron model overrides, use explicit provider/model IDs to avoid unintended provider fallback routing.
- Keep local-only git workflow (no remote push) unless user explicitly asks.

## Open questions / risks
- Policy category (`🏛️ 政策`) can occasionally return insufficient fresh items under strict filtering; may need source/query tuning.
- `Manager healthcheck` frequency remains every 5 minutes; still potentially noisy though now healthy.
- Global model allow/route behavior may still differ between sessions if unqualified model IDs are used.

## Next actions
- Owner: Manager — tighten briefing freshness from 48h to 24h when user approves.
- Owner: Manager — add stale-news ratio gate + auto-retry before delivery when user approves.
- Owner: User/Manager — decide whether to reduce healthcheck cadence (e.g., 15m) for lower churn.

## Artifacts
- Code: `/home/moonlight/.openclaw/workspace/daily-briefing/fetch-rss-v1.js`
- Commit: `7d12750` (`daily-briefing: enforce freshness filter and safer RSS fallback`)
- Cron job fixed: `4c3c7c7f-87c5-4df3-9b89-60e2676dcf63` (`Manager healthcheck`)
- Config touched: `~/.openclaw/openclaw.json` (model alias/route context)
- Keywords: `daily-briefing`, `rss`, `freshness-filter`, `cron`, `model-not-allowed`, `ollama`, `qwen2.5:3b`

---

## Summary (Session Wrap-up Addendum)
- Completed OpenViking local installation and validated end-to-end CLI workflow.
- Switched embedding backend to host Ollama (`http://192.168.79.1:11434/v1`) with `mxbai-embed-large`.
- Enabled persistent OpenViking autostart via user systemd service.
- Added global agent policy for OpenViking layered retrieval (`search → abstract → overview → read`) to reduce token usage.
- Verified layered context retrieval in practice (L0/L1/L2 + semantic search).

## Decisions
- Use host Ollama as default embedding provider to avoid paid API dependency.
- Keep OpenViking key as non-sensitive placeholder (`local-noauth`) for local no-auth endpoint.
- Enforce token-saving retrieval order across all agents via policy docs.

## Open questions / risks
- If host IP changes, OpenViking embedding `api_base` must be updated.
- Local embedding quality/latency depends on host machine load and model availability.
- Semantic queue can degrade if host Ollama is temporarily unreachable.

## Next actions
- Owner: Manager — monitor `openviking.service` stability after reboot.
- Owner: User/Manager — rotate previously exposed external keys (OpenAI/AKSK) if still active.
- Owner: Manager — optionally add lightweight health-check cron for OpenViking + host Ollama reachability.

## Artifacts
- OpenViking config: `~/.openviking/ov.conf`
- OpenViking CLI config: `~/.openviking/ovcli.conf`
- Service unit: `~/.config/systemd/user/openviking.service`
- Policy updates: `/home/moonlight/.openclaw/workspace/AGENTS.md`, `/home/moonlight/.openclaw/workspace/TOOLS.md`
- Commit: `b3d6e06` (`agent-policy: enforce OpenViking layered retrieval for all agents`)
- Test docs: `tmp/context_compress_demo.md`, `tmp/openviking_ollama_test.md`
- Keywords: `openviking`, `ollama`, `mxbai-embed-large`, `layered-context`, `token-saving`, `systemd-user-service`

---

## Summary (Archive: OpenViking/qmd conflict policy)
- Clarified that OpenViking is already active in this workspace (service + data present).
- Corrected prior mismatch: OpenViking exists as local retrieval layer, not as a skill entry.
- Implemented conflict-safe memory routing policy: OpenViking primary, qmd secondary.
- Added evidence precedence and degraded fallback order to avoid retrieval conflicts.
- User requested archival (`归档`) after configuration update.

## Decisions
- Default retrieval remains OpenViking layered flow: `search → abstract → overview → read`.
- qmd is limited to cross-checking, quote extraction, and path pinpointing.
- Conflict arbitration order: raw/source read > qmd snippet > abstract/overview; prefer newer timestamp.

## Open questions / risks
- qmd quality still depends on indexing freshness; stale index can create apparent conflicts.
- If OpenViking endpoint is down, response quality may temporarily drop in degraded mode.

## Next actions
- Owner: Manager — on next retrieval task, validate policy execution traces once (OpenViking-first + qmd-second).
- Owner: User/Manager — decide whether to add periodic qmd reindex schedule.

## Artifacts
- Updated policy: `/home/moonlight/.openclaw/workspace/AGENTS.md`
- Updated local notes: `/home/moonlight/.openclaw/workspace/TOOLS.md`
- Commit: `394d731` (`memory-routing: set OpenViking primary, qmd secondary arbitration`)
- Keywords: `归档`, `openviking`, `qmd`, `arbitration`, `memory-routing`, `degraded-mode`

---

## Summary (Archive: SearXNG install + agent web-search integration)
- Installed SearXNG locally via Docker stack (`caddy + searxng + redis`) under workspace.
- Fixed bootstrap failures caused by default `ultrasecretkey` and limiter behavior by using a dedicated writable config mount and explicit settings.
- Verified service availability (`http://localhost/`), HTML search, and JSON API search endpoint.
- Added unified CLI wrapper `tools/web-search` with text and JSON modes for reusable agent calls.
- Updated agent policy docs so the agent proactively uses SearXNG for fresh external lookups when appropriate.

## Decisions
- Use local SearXNG as primary lightweight web-search backend before ad-hoc browser scraping for general fact lookup.
- Keep Docker restart policy `unless-stopped` + rely on host Docker enabled startup for boot persistence.
- Maintain local-only workflow (no remote push) unless explicitly requested by user.

## Open questions / risks
- Some public engines can intermittently trigger CAPTCHA/rate limits; result quality may vary per source engine.
- If Docker service is disabled manually, SearXNG will not auto-start until Docker is restored.

## Next actions
- Owner: Manager — optionally tune enabled engines for stability (prefer Brave/Startpage/Wikipedia for fewer CAPTCHAs).
- Owner: User/Manager — decide whether to expose SearXNG beyond localhost with reverse proxy + auth.

## Artifacts
- Stack: `/home/moonlight/.openclaw/workspace/searxng-docker`
- Config mount in use: `/home/moonlight/.openclaw/workspace/searxng-docker/searxng-agent2/settings.yml`
- Wrapper: `/home/moonlight/.openclaw/workspace/tools/web-search`
- Backward wrapper: `/home/moonlight/.openclaw/workspace/tools/searx-search.sh`
- Policy updates: `/home/moonlight/.openclaw/workspace/AGENTS.md`, `/home/moonlight/.openclaw/workspace/TOOLS.md`
- Keywords: `searxng`, `web-search`, `docker-compose`, `format=json`, `agent-tooling`, `归档`
