# 2026-03-06 Session Log

## Summary

**任务**: SELF_REPORT_ALIGNMENT 协议 Phase A + B 实施 + Shadow Mode 启动

**关键成果**:
- Phase A 验证质量完成（149 tests）
- Phase B Shadow Mode 实施完成（50 tests）
- SRAP 集成到 emotiond-enforcer hook
- Shadow Mode 正式启动，开始收集真实数据

## Decisions

### 1. 测试统计口径修正
- Phase A: 149 tests (77 + 40 + 32)，非之前报告的 101
- Phase B: 50 tests
- 总计: 199 tests

### 2. 测试数据隔离
- 17 条测试数据已归档到 `test_data_archive/`
- shadow_log.jsonl 已清空，等待真实数据

### 3. SRAP 环境变量配置
- 通过 systemd 配置 `SRAP_ENABLED=1`, `SRAP_MODE=shadow`
- 避免直接修改 .bashrc，使用服务级别配置

### 4. 默认 mode 确认
- interpreter 默认 mode = interpreted
- numeric mode 不会进入默认链路
- 符合安全要求

## Key Artifacts

### 测试文件
- `tests/test_adversarial_self_report.py` (77 tests) - 5类攻击模式
- `tests/test_e2e_replay.py` (40 tests) - E2E 确定性验证
- `tests/test_self_report_metrics.py` (32 tests) - FP/FN 统计
- `tests/test_shadow_mode.py` (50 tests) - Shadow 模式测试

### 核心文件
- `emotiond/self_report_check.py` - CLI wrapper
- `emotiond/self_report_consistency_checker.py` - 审计器 (v2.0)
- `emotiond/self_report_interpreter.py` - 解释器
- `emotiond/self_report_metrics.py` - 指标收集
- `emotiond/shadow_analyzer.py` - 报告生成器

### 集成文件
- `integrations/openclaw/hooks/emotiond-enforcer/handler.js` - Hook 集成
- `~/.config/systemd/user/openclaw-gateway.service` - 环境变量配置

### 文档
- `SHADOW_DEPLOYMENT_GUIDE.md` - 部署指南
- `SRAP_MONITORING_GUIDE.md` - 监控指南
- `artifacts/self_report/shadow_log.jsonl` - Shadow 日志

### 工具
- `~/.openclaw/workspace/tools/srap-daily-report` - 每日报告生成器
- `~/.openclaw/workspace/tools/srap-start-shadow` - 启动脚本

## Open Questions

1. **Phase C 触发条件**: 
   - violation_rate < 5% 的阈值是否合理？
   - FP/FN 需要多少人工复核样本才能可信？

2. **Shadow 观察期**:
   - 3-7 天是否足够？
   - 如何确保样本量达到 200？

3. **numeric_leak 监控**:
   - 如何区分真实 numeric leak 和测试数据污染？

## Next Actions

| 动作 | 负责人 | 状态 |
|------|--------|------|
| Shadow 观察 3-7 天 | 系统 | 🏃 运行中 |
| 每日检查 shadow_log | 用户/agent | ⏳ 待执行 |
| 样本量 ≥200 后评估 Phase C | agent | ⏳ 等待 |
| 人工复核 FP/FN | 用户 | ⏳ 待执行 |

## Keywords

`SRAP` `self-report` `shadow-mode` `emotiond` `validation` `E2E` `adversarial-testing` `FP-FN-metrics` `phase-b` `integration`

## Workflow State

```json
{
  "active": false,
  "phase": "B",
  "steps": [
    {"id": "phase_a", "status": "done", "tests": 149},
    {"id": "phase_b", "status": "done", "tests": 50},
    {"id": "srap_integration", "status": "done"},
    {"id": "shadow_startup", "status": "done", "sample_count": 0}
  ]
}
```

---

## MVP11.6 Phase 1 完成 (2026-03-06 08:50 CST)

### 完成任务

| Task | 状态 | 产物 |
|------|------|------|
| Task 3: Enforcement Matrix | ✅ | POLICIES/ENFORCEMENT_MATRIX.md (230行) |
| Task 5: Response Path Coverage | ✅ | tools/check-response-path-coverage |
| Task 6: Gate接入 | ✅ | docs/MVP11_6_GATE_READINESS.md |
| Task 7: Rollout Plan | ✅ | docs/SHADOW_TO_ENFORCED_ROLLOUT.md (246行) |

### 关键决策

1. **Enforcement Matrix 分级**: P0 (hard_block) / P1 (block_candidate) / P2 (shadow_only)
2. **Response Path Coverage**: 当前 25% (1/4)，需迁移 subagent_response
3. **Rollout 计划**: Shadow → C0 → C1 → C2 → C3，预计 60-120 天

### 数据依赖任务

- Task 1: Shadow Hardening Window (运行中，需 3-7 天)
- Task 2: Violation Taxonomy (等待样本 ≥200)
- Task 4: Phase C Decision (等待 Task 2)
- Task 8: Testbot Consistency (等待样本 ≥200)

### 工具

- `~/.openclaw/workspace/tools/check-response-path-coverage` - 路径覆盖检查
- `~/.openclaw/workspace/tools/mvp11-6-daily-check` - 每日进度检查

### 下一步

- 等待 Shadow 数据采集
- 每日运行 mvp11-6-daily-check
- 样本 ≥200 后执行 Task 2, 4, 8


---

## Phase 1.5 Coverage Closure 完成 (2026-03-06 08:48 CST)

### 用户反馈

用户指出 **Coverage 25% 比 sample=0 更严重**：
- 只有 1/4 path 被治理 → 收集的数据有"局部正确"风险
- subagent_response 是高风险路径，应该 P0 迁移
- Phase C 决策应该考虑 coverage，不只是样本数

### 执行结果

| Task | 状态 | 产物 |
|------|------|------|
| Task 1.5A: subagent_response migration | ✅ | tools/subagent-srap-check |
| Task 1.5B: Coverage-aware metrics | ✅ | tools/mvp11-6-daily-check v2.0 |
| Task 1.5C: Phase C validity rules | ✅ | POLICIES/PHASE_C_VALIDITY_RULES.md |

### 关键改进

1. **Coverage 从 25% → 无 Category C**
   - subagent_response: Category C → Category B (50%)
   - 添加 checker + shadow_log 组件
   - 工具：`tools/subagent-srap-check`

2. **Phase C 门禁规则**
   - 样本数 ≥200 **且** 覆盖率 ≥75%
   - 不满足则数据无效
   - 输出：`artifacts/self_report/phase_c_gate.json`

3. **每日检查增强**
   - Path-level sample counts
   - Coverage ratio tracking
   - Phase C validity check

### 当前状态

| 指标 | 值 | 目标 | 状态 |
|------|-----|------|------|
| Sample count | 0 | ≥200 | ⏳ |
| Coverage rate | 25% | ≥75% | ⏳ |
| Category C paths | 0 | 0 | ✅ |

### 下一步

- 继续 Shadow 数据采集
- 完成剩余 coverage (interpreter/contract for subagent_response)
- 样本达标后执行 Task 2/4/8


---

## Execution Policy v2.1 完成 (2026-03-06 10:19 CST)

### 核心成果

完成从"防忘记"到"防篡改"的根本升级：

**v1.0 → v2.0 → v2.1 演进**：
- v1.0: 机制建立（verify-and-close + done-guard）
- v2.0: 边界封口（对抗测试 + 退化监控 + CI 门禁）
- v2.1: 信任锚（签名链 + Gate 分权 + State Authority）

### 工具链

| 工具 | 用途 | 状态 |
|------|------|------|
| `receipt-signer` | HMAC 签名 | ✅ |
| `gate-a/b/c-signer` | Gate 独立签发 | ✅ |
| `final-aggregator` | 只能聚合，不能伪造 | ✅ |
| `state-authority` | 状态机权威 | ✅ |
| `verify-and-close-v2` | Trust Anchor 集成 | ✅ |

### 测试结果

- 正常测试：13/13 通过
- 对抗测试：9/9 阻止
- Happy Path：完整通过
- Attack Path：2/2 阻止（篡改检测 + 状态机绕过）

### Trust Anchor 架构

```
Gate A Signer (独立签发)
     ↓
Gate B Signer (独立签发)
     ↓
Gate C Signer (独立签发)
     ↓
Final Aggregator (只能聚合)
     ↓
State Authority (状态迁移裁决)
     ↓
finalize-response / done-guard / safe-message
     ↓
用户消息输出
```

### Baseline 产物

位置：`artifacts/baseline_v2.1/`

- Happy path receipts (签名验证通过 )
- Attack samples (篡改检测样本 )
- BASELINE_SUMMARY.md
- metrics_snapshot.json

### 四项完成度

1. ✅ Receipt 签名链 - HMAC 签名，只验证核心字段
2. ✅ Gate signer 分权 - A/B/C 独立签发，final 只能聚合
3. ✅ State authority 独立化 - Receipt ≠ State Transition
4. ⏳ 原生 message hook - 需 OpenClaw 核心集成

### 关键决策

- 签名只对核心字段（不含 checks/issues）
- 每个 Gate 独立 signer_id
- State Authority 拒绝 standalone 状态的 ready_to_close 迁移
- Baseline 样本保留，不删除 golden fixtures

### 关键词

`execution-policy` `trust-anchor` `HMAC-signature` `gate-signer` `state-authority` `adversarial-testing` `baseline-freeze`

---

## Execution Policy v2.1 冻结 + 默认脚手架 (2026-03-06 10:30 CST)

### 核心成果

**三项完成**:

1. ✅ **版本冻结**
   - `docs/releases/TRUST_ANCHOR_v2.1_HANDOFF.md` (架构文档)
   - `artifacts/baseline_v2.1/BASELINE_SUMMARY.md` (测试摘要)
   - `docs/releases/VERSION_TAGS.jsonl` (版本标签)

2. ✅ **原生 Hook 状态澄清**
   - OpenClaw 已有 `message:sending` hook 系统
   - `emotiond-enforcer` 已在工作
   - 只需创建 `execution-policy-enforcer` hook
   - **不需要** OpenClaw 核心修改

3. ✅ **默认脚手架升级**
   - `install-execution-policy` v2.1
   - 新项目自动获得 Trust Anchor 链
   - 包含原生 hook scaffold (shadow mode)

### 关键发现

**原生 hook 已存在且在工作**：

```
~/.openclaw/hooks/emotiond-enforcer/
├── handler.js  ← message:sending 中间件
├── hook.json
└── HOOK.md
```

**安装内容** (新项目自动获得):

| 组件 | 数量 | 说明 |
|------|------|------|
| Trust Anchor 工具 | 8 | receipt-signer, gate-a/b/c, final-aggregator, state-authority, verify-and-close-v2, safe-message |
| Schema | 2 | task_receipt.v1, doctor_report.v1 |
| 测试 | 2 | 正常 + 对抗 |
| Hook | 1 | execution-policy-enforcer (shadow) |
| CI | 1 | execution-policy-gate.yml |
| 文档 | 3 | HANDOFF, INTEGRATION, SCAFFOLD |

### 工具

- `tools/install-execution-policy` - v2.1 安装器
- `hooks/execution-policy-enforcer/handler.js` - Hook scaffold

### 文档

- `docs/releases/TRUST_ANCHOR_v2.1_HANDOFF.md` - 架构冻结
- `docs/releases/NATIVE_HOOK_INTEGRATION.md` - Hook 集成路线图
- `docs/EXECUTION_POLICY_DEFAULT_SCAFFOLD.md` - 默认脚手架说明

### 关键词

`execution-policy` `trust-anchor` `frozen` `native-hook` `scaffold` `v2.1` `default-template`

---

## Context Compression Pipeline 完成 (2026-03-06 11:17 CST)

### 核心成果

完成 OpenClaw 日常聊天上下文压缩基础设施：

**4 阶段执行**：
- Phase 0: Schema 冻结 (4 schemas + 1 policy)
- Phase 1: Core Tools (budget-check + capsule-builder)
- Phase 2: Compression + Retrieval (compress + retrieve)
- Phase 3: Enforced + Metrics (prompt-assemble + shadow-report)

**交付物**：
- 4 个 JSON Schema
- 1 个政策文档 (92 行)
- 6 个工具 (5 healthy, 1 degraded)

### 关键架构

**三层上下文模型**：
1. Resident Layer - 常驻（任务目标、open_loops、硬约束）
2. Active Layer - 活动（最近 6-12 轮对话）
3. Recall Layer - 回填（capsule + vector 检索）

**压缩阈值**：
- 70% → Light (shadow only)
- 85% → Standard (12-turn window)
- 92% → Strong (4-6 turns, full retrieval)

### Metrics Gate

```yaml
ready_for_enforced:
  - old_topic_recovery_rate >= 0.85
  - commitment_preservation_rate >= 0.90
  - false_compression_rate < 0.05
  - hallucinated_commitment_rate < 0.02
```

### 下一步

1. Shadow Observation (3-7 天)
2. 运行 `context-shadow-report --days 7`
3. 评估 readiness 后启用 Light Enforced

### 关键词

`context-compression` `session-capsule` `three-layer-model` `shadow-mode` `resident-state`

---

## Context Compression Rollout Policy (2026-03-06 11:22 CST)

### 用户反馈

**核心判断**：工程实现完成，进入验证期；距离可正式默认启用，还差 retrieve 闭环验证。

**关键问题**：
- `context-retrieve` = degraded (OpenViking unavailable)
- retrieve 是压缩闭环的一半，不是可选件

### 新增内容

**Gate 0**: context-retrieve 必须健康才能进入 Enforced

**新增指标**：
- `rehydration_precision >= 0.80` - 回填内容中真正相关且没带偏的比例

**保护机制**：
1. Compression Kill Switch (CONTEXT_COMPRESSION_ENABLED/MODE)
2. Never-Compress Slots (task_goal, open_loops, recent_commitments, etc.)

**场景分层**：
- ✅ 允许：单主题聊天、无工具链、无强承诺
- ❌ 阻断：长任务协作、多 agent、修 bug、技术对话

**回归测试集**：
- `tests/context/test_old_topic_recall.py` (4/4 passed)

### 下一步

1. 修复 context-retrieve (启用 OpenViking)
2. Shadow Observation (100-200 samples)
3. 评估 rehydration_precision
4. 达标后进入 Light Enforced

### 关键词

`rollout-policy` `gate-0` `rehydration-precision` `kill-switch` `never-compress-slots`

---

## Context Compression v2.0 - Two-Tier Retrieval (2026-03-06 11:30 CST)

### 核心改进

**两级检索架构**：
- L1: Capsule Fallback (always available, confidence 0.85)
- L2: Vector Enhancement (optional, confidence 0.65)
- 显式来源标签：source, tier, confidence

**当前状态**：
- L1: ✅ 可用
- L2: ❌ OpenViking unavailable
- Overall: `degraded_but_functional`

### Gate 系统

| Gate | 条件 | 状态 |
|------|------|------|
| Gate 0 | L1 可用 | ✅ 通过 |
| Gate 1 | L1+L2 健康 | ❌ 阻塞 |

### Shadow Observation 分两阶段

**Phase S1** (当前可启动):
- 模式: `--capsule-only`
- 样本: 80+ (6 桶强制分桶)
- 指标: capsule_only_old_topic_recovery >= 0.70

**Phase S2** (等 OpenViking):
- 模式: full retrieval
- 复用 S1 样本
- 指标: old_topic_recovery_rate >= 0.85

### 新增指标

- `commitment_drift_rate < 0.05`
- `capsule_only_old_topic_recovery >= 0.70`
- `capsule_only_commitment_preservation >= 0.85`

### 下一步

1. ✅ Gate 0 已通过
2. ⏳ 启动 Phase S1 Shadow
3. ⏳ 收集 80+ 样本
4. ⏳ 修复 OpenViking 后 Phase S2

### 关键词

`two-tier-retrieval` `fallback-gate` `phase-s1` `capsule-only`

---

## Context Compression v2.1 - S1 Ready (2026-03-06 11:38 CST)

### 核心突破

**系统已具备最小闭环**：
- L1 保底可恢复，L2 负责增强
- 不再是单向丢弃系统
- Gate 0 已通过，可启动 Phase S1

### Gate 系统 (固定)

| Gate | 条件 | 状态 |
|------|------|------|
| Gate 0 | Minimal Closure | ✅ PASSED |
| Gate 1 | Capsule Ready | ⏳ PENDING |
| Gate 2 | Full Retrieval | ❌ BLOCKED |

### S1 验收面板 (6 指标)

**核心指标** (必须达标):
- capsule_only_old_topic_recovery >= 0.70
- capsule_only_commitment_preservation >= 0.85
- commitment_drift_rate < 0.05

**质量指标** (观测):
- priority_preservation_rate >= 0.80
- compression_gain_ratio >= 0.30
- source_mix_distribution (capsule > 90%)

### 4 个风险点

1. Capsule 召回命中但内容漂了 → commitment_drift_rate
2. Capsule 过于保守压缩收益不够 → compression_gain_ratio
3. Open loops 保住但优先级丢了 → priority_preservation_rate
4. Capsule-only 模式下误回填 → source_mix_distribution

### 保护机制

**低 confidence 保护规则**:
- confidence >= 0.60: 正常进 recall 层
- confidence < 0.60: 只能进 low_confidence 区，不可覆盖 resident

**保护字段**: task_goal, open_loops, recent_commitments, tool_execution_state, recent_errors, user_corrected_constraints, response_contract

### S1 执行计划

**样本收集**:
- 真实日常会话: 40+
- 定向压力样本: 50+ (6 个场景)
- 总计: 90+ samples

**压力测试**: 6/6 passed

### 下一步

1. ✅ Gate 0 已通过
2. ⏳ 收集 80+ 样本
3. ⏳ 计算 6 个指标
4. ⏳ Gate 1 评估
5. ⏳ 可进入 Light Enforced (capsule-only)

### 工具

- `s1-dashboard` - S1 监控面板
- `context-retrieve --capsule-only` - L1 强制模式
- `tests/context/stress/test_s1_scenarios.py` - 压力测试

### 关键词

`phase-s1` `minimal-closure` `low-confidence-protection` `commitment-drift` `compression-gain`

---

## Context Compression S1 Feature Freeze (2026-03-06 11:50 CST)

### 核心规则

**S1 期间除 blocker 修复外，禁止功能变更。**

参见：`POLICIES/S1_FEATURE_FREEZE.md`

### 原因

S1 是证据采集阶段，不是开发阶段。

问题：样本期数据会被污染，最后不知道指标变化是因为：
- 压缩策略本身
- capsule 质量
- prompt assemble
- 还是中途改了规则

### Gate 1 条件升级

**双条件**：

1. **分桶达标**：
   - 总样本 >= 80
   - 每个桶都达到最小样本数
   - 不能用简单样本冲总量

2. **连续窗口达标**：
   - 最近 30 个样本也要达标
   - 防止被早期简单样本拉高

### 分桶最小样本

| 桶 | 最小样本 |
|---|---------|
| old_topic_recall | 15 |
| multi_topic_switch | 12 |
| with_open_loops | 12 |
| post_tool_chat | 10 |
| user_correction | 10 |
| long_technical | 8 |

### 风险：假稳定

系统在普通对话里稳定，一到复杂场景就软漂。

**监控**：按桶 drift 分布，找到真正薄弱点。

### Light Enforced 分步 Rollout

**Step A**：capsule-only，只放低风险会话
**Step B**：扩大到一般日常会话

不要 Gate 1 一过就全量切换。

### 监控工具

`s1-dashboard` - 显示分桶覆盖率、连续窗口、薄弱点检测

### 当前状态

```
Gate 0: ✅ PASSED
Gate 1: ⏳ PENDING (blockers: total_samples, bucket_coverage, recent_window_size)
样本: 12 / 80
分桶: 全部未达标
连续窗口: 12 / 30 (不足)
```

### 关键词

`feature-frozen` `bucket-coverage` `continuous-window` `false-stability` `controlled-rollout`

---

## S1 Sample Counting Rules Fixed (2026-03-06 11:58 CST)

### 核心规则

**1. 多标签样本规则**：
- 允许多标签
- 分开统计：唯一样本数 vs 桶命中数
- 防止"复杂样本冲高覆盖率"

**2. Blocker 白名单**（只有 3 类）：
- sampling_blocked: 导致 S1 无法继续采样
- metrics_invalid: 导致指标计算失真
- protection_failed: 导致 resident/open_loops/commitments 保护失效

**3. 实验面冻结**（不只是代码）：
- schemas 版本
- metrics 公式
- prompt 模板
- bucket tagging 规则
- dashboard 口径

**4. 自由样本规则**：
- 优先补到最弱桶
- 不允许连续 10 个以上样本来自非薄弱桶

**5. S1 Baseline Manifest**：
- `artifacts/context_compression/S1_BASELINE_MANIFEST.json`
- 记录 commit hash, schema version, scoring version
- 用于版本一致性检查

### 自由样本空间

分桶总和: 67
总样本要求: 80
**自由空间: 13**

用途: 补到当前最弱桶

### 文件

- `POLICIES/S1_SAMPLE_COUNTING_RULES.md`
- `artifacts/context_compression/S1_BASELINE_MANIFEST.json`

### 关键词

`sample-counting` `multi-label` `blocker-whitelist` `experimental-freeze` `baseline-manifest`
