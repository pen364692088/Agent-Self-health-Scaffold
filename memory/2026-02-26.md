# 2026-02-26 Session Log

## Summary
- Researched reliable free LLM API providers and validated candidates with live checks.
- Integrated OpenRouter into local OpenClaw config and auto-imported all currently available `:free` models.
- Verified OpenRouter connectivity and identified per-model status patterns (PASS / RATE_LIMIT / transient errors).
- Reworked default model routing per user request: daily default moved back to `iflow/glm-4.6`.
- Built a cross-provider fallback chain using iFlow + NVIDIA NIM + OpenRouter free models, ordered by practical quality/availability.
- Confirmed gateway health after config changes (`RPC probe: ok`).

## Decisions (what changed + why)
- Kept **default model** as `iflow/glm-4.6` because user preference is strongest and quality is stable in this environment.
- Kept **OpenRouter enabled as a route/fallback pool** instead of primary, because free-tier rate limits fluctuate.
- Added mixed-provider fallbacks to reduce single-provider outages and improve continuity.
- Kept `Codex-5` (`openai-codex/gpt-5.3-codex`) available for complex/deep reasoning tasks.

## Open questions / risks
- OpenRouter free models can frequently hit 429 rate limits depending on time/provider load.
- Some NIM models still show intermittent unavailability in this environment (`glm5`, `kimi-k2.5`, llama-4 variant).
- Config now contains sensitive API keys in plain text; operationally convenient but higher secret-exposure risk.

## Next actions (owner + smallest next step)
- Owner: Manager — run periodic lightweight availability probes and reorder fallback list if pass rates drift.
- Owner: User/Manager — decide whether to move API keys to env-only references (no plaintext in config).
- Owner: Manager — when requested, add profile-based routing presets (e.g., coding vs. drafting vs. long-context).

## Artifacts (paths/commands)
- Config: `/home/moonlight/.openclaw/openclaw.json`
- Command used for provider checks: Python `chat/completions` probes against iFlow, NIM, OpenRouter.
- Gateway validation: `openclaw gateway status`
- Keywords: `openrouter`, `iflow`, `nvidia-nim`, `fallback-routing`, `free-models`, `rate-limit`, `glm-4.6`

---

## Summary
- Checked Antfarm run status repeatedly and confirmed run `#6 / 630db4f8` stalled and was auto-cancelled by Medic.
- Restarted the same task as new run `#7 / 914c860d` because cancelled runs cannot be resumed.
- Implemented code change in Antfarm Medic so stalled runs are marked `failed` (resumable) instead of ending up on cancel path.
- Added operator playbook rule: on Medic alert, try `workflow resume <run-id>` first; only `workflow stop` when intentionally abandoning.
- Delivered morning daily briefing to Telegram and kept workflow monitoring active.

## Decisions (what changed + why)
- Switched stalled-run handling to **fail+resume-first** to preserve completed story progress and avoid full restart cost.
- Kept `workflow stop` as explicit abandon-only action, since in current Antfarm behavior it is effectively cancel and non-resumable.
- Persisted this as durable ops policy in `TOOLS.md` to standardize future incident response.

## Open questions / risks
- Need one real stalled-run incident to verify end-to-end: Medic marks failed → `workflow resume` recovers from failed step.
- Existing cancelled runs (like `#6`) are still non-recoverable and must be rerun.
- Antfarm build touched `package-lock.json` version metadata in local repo; not included in patch commit yet.

## Next actions (owner + smallest next step)
- Owner: Manager — on next Medic stall alert, run `workflow resume <run-id>` immediately and validate recovery path.
- Owner: Manager — monitor run `#7` and intervene only if no progress exceeds stall threshold.
- Owner: User/Manager — decide if we should also add a configurable Medic mode switch (`fail|cancel|alert-only`) in Antfarm CLI.

## Artifacts (paths/commands)
- Antfarm patch commit: `/home/moonlight/.openclaw/workspace/antfarm` @ `cd22ed0`
- Workspace policy commit: `/home/moonlight/.openclaw/workspace` @ `068fa71`
- Updated files:
  - `antfarm/src/medic/checks.ts`
  - `antfarm/src/medic/medic.ts`
  - `TOOLS.md`
- Key commands:
  - `node ~/.openclaw/workspace/antfarm/dist/cli/cli.js workflow status <run>`
  - `node ~/.openclaw/workspace/antfarm/dist/cli/cli.js workflow resume <run-id>`
  - `node ~/.openclaw/workspace/antfarm/dist/cli/cli.js medic run`
- Keywords: `antfarm`, `medic`, `stalled-run`, `resume-first`, `workflow-stop`, `run-630db4f8`, `run-914c860d`
