# Session Log - 2026-03-03

**Status**: ARCHIVED ✅
**Archived at**: 2026-03-03 15:46 CST (Final)

---

## 今日完成

### 1. Scoring-Canary 完整实现 (11:39 - 14:40)

- Scope Normalization 三态策略
- Canary 6-Point Check 全自动判定
- Score 分布 + 组件 P50
- Hysteresis 自动推荐
- sim 自动检测

### 2. memory.md 精简 (14:49 - 14:55)

| 指标 | Before | After |
|------|--------|-------|
| 大小 | 64KB | 4KB |
| 减少 | - | **94%** |

### 3. 检索增强 v1.0 (15:03 - 15:11)

- Pinned Pointers (10 条黄金入口)
- Two-Stage Retrieval (pinned + semantic)
- Tombstone 双过滤
- 检索审计事件

### 4. Retrieval Hardening v1.0 (15:16 - 15:23) 🚀

**cc-godmode 执行完成**

| 增强 | 实现 |
|------|------|
| Pinned 限制 | 最多 3 条 + hits_count warn |
| Stage2 预算 | max_docs=5, min_score=0.75, deterministic |
| Superseded | redirect 结构 + audit 追踪 |
| Audit 采样 | 10% + 异常 100% + daily summary |

**产物**:
- `tools/memory-retrieve` (hardened)
- `memory/tests/retrieval_regression.json` (7 测试)
- `tools/retrieval-regression-runner`
- 定时任务: 每 6h 运行回归测试

### 5. Use Link Audit + Tuning Recommendations (15:30 - 15:45)

**C 方案完整执行**

| Step | 内容 | 状态 |
|------|------|------|
| 1 | use-link-audit 工具 | ✅ |
| 2 | tuning_recommendations 5 条规则 | ✅ |
| 3 | 扩容门槛新增 use 链路条件 | ✅ |
| 4 | use 事件链路修复 | ✅ |
| 5 | 语义纠偏 (recall vs adoption) | ✅ |

**关键修复**:
- recall 事件: retrieval 时发射 (不计入评分)
- use 事件: adoption 时发射 (计入 use_term)

**新增指标**:
| 指标 | 含义 | 合理区间 |
|------|------|---------|
| adoption_rate | use / recall | 10-80% |
| use_attribution | memory_id 正确率 | ≥ 99% |

---

### Commit 链

| Commit | 内容 |
|--------|------|
| `8facc72` | Use event semantics - recall vs adoption |
| `f323ebd` | 记录 .openclaw 写入规则 |
| `83312a8` | Use event chain - emit on every retrieval |
| `dafde2d` | Use Link Audit + Tuning Recommendations |
| `8362a20` | Retrieval Hardening v1.0 |
| `5775a6b` | Session archive 2026-03-03 |

---

### 关键文件

| 文件 | 用途 |
|------|------|
| `tools/memory-retrieve` | Hardened retrieval v1.1 (recall/use 分离) |
| `tools/use-link-audit` | 链路验证 + adoption_rate |
| `tools/memory-sweeper` | v2.0 + tuning_recommendations |
| `tools/retrieval-regression-runner` | 定时回归测试 |
| `memory/tests/retrieval_regression.json` | 7 测试用例 |

---

### 观测期 (24-72h)

- **截止**: 2026-03-06 15:45 CST
- **监控**:
  - adoption_rate 进入合理区间 (10-80%)
  - use_term_p50 > 0 (真实学习信号)
  - 回归测试 7/7 通过
  - CANARY_STATUS 稳定

---

### 关键词

`use-event-semantics` `recall-vs-adoption` `adoption-rate` `retrieval-hardening` `tuning-recommendations` `memory-citation-protocol`

---

## 稳态验证清单

- [x] 回归测试 7/7 通过
- [x] 定时任务已安装 (每 6h)
- [x] recall/use 语义分离
- [x] adoption_rate 指标可用
- [ ] 72h 稳态观察 (截止 2026-03-06)
- [ ] use_term_p50 > 0 (真实学习信号)
- [ ] adoption_rate 进入合理区间

---

## Memory Retrieval Hardening v1.2 → v1.2.6 (2026-03-03 15:52 - 20:09 CST)

### 完成的工作

**v1.2** (16:06-18:25)
- Adoption 指标拆分：`adoption_rate_any`, `uses_per_recall`, `candidate_adoption_rate`
- Use 事件结构化输出：`parse-used-memory` + AGENTS.md 规则
- 语义版本闸门：`event_version` 字段，v2 过滤
- 监控面板：`memory-dashboard` 四件套指标

**v1.2.1** (18:32-18:35)
- 强一致回归测试：`tests/test_memory_retrieval_hardening.py` (6 tests)
- 指标口径细化：样本量、unique/raw、v1/v2 分区
- 风险告警：`memory-risk-alerts` 5 阈值

**v1.2.2** (18:54-18:58)
- 样本量门槛：MIN_EVENTS=200, MIN_SESSIONS=20
- 分阶段阈值：Day 1 grace(25%), Day 2+ strict(50%)
- orphan 指标：`orphan_use_rate`, `orphan_recall_rate`
- 自动化脚本：`memory-daily-obs`

**v1.2.3** (19:04-19:07)
- 报告版本化：report_version, git_commit, config_hash
- 可消费摘要：`obs_YYYY-MM-DD.md`
- CI 集成：`ci-memory-hardening` + GitHub Actions

**v1.2.4** (19:24-19:35)
- config_hash 可复现：只包含有效配置
- orphan 拆分：`unadopted_recall_rate` vs `broken_link_recall_rate`
- 快照回归：`expected_*.json` + CI diff
- 维护文档：`memory/MAINTENANCE.md`

**v1.2.5** (19:37-19:43)
- 场景分桶：Bucket A (应该被采用) vs Bucket B (未采用正常)
- 豁免项：`linkability` 字段，只统计 expected

**v1.2.6** (20:00-20:08)
- 漂移检测：`bucket_a_ratio` < 70% 中位数 → WARN
- Linkability Coverage：`expected_ratio` < 70% → WARN
- 历史基线：7 日滚动中位数

### 工具链

| 工具 | 版本 | 用途 |
|------|------|------|
| `memory-dashboard` | v1.2.6 | 监控面板 |
| `memory-risk-alerts` | v1.2.6 | 风险告警 |
| `memory-daily-obs` | v1.2.4 | 每日观测 |
| `ci-memory-hardening` | v1.2.3 | CI 入口 |
| `test_memory_retrieval_hardening.py` | 8 tests | 回归测试 |

### 关键决策

1. **语义闸门**：v1/v2 分离，避免历史脏数据污染新指标
2. **分阶段阈值**：第 1 天 grace，第 2+ 天 strict
3. **场景分桶**：只对 Bucket A 执行严格告警
4. **相对基线**：使用 7 日中位数作为漂移检测基线

### 告警规则（最终版）

| 指标 | 条件 | 级别 |
|------|------|------|
| `unadopted_rate_bucket_a` | > 10% | WARN |
| `broken_link_recall_rate` | > 5% (expected) | ERROR |
| `bucket_a_ratio` | < 70% 中位数 | WARN |
| `expected_ratio` | < 70% 或 < 85% 中位数 | WARN |

### 运行命令

```bash
# 每日观测
~/.openclaw/workspace/tools/memory-daily-obs

# CI 检查
~/.openclaw/workspace/tools/ci-memory-hardening

# 回归测试
python3 ~/.openclaw/workspace/tests/test_memory_retrieval_hardening.py
```

### Commits

- `92d872f` - v1.2 初始实现
- `b271839` - v1.2.1 回归测试
- `cd6762d` - v1.2.2 样本量门槛
- `b33ea77` - v1.2.3 CI 集成
- `1dd017b` - v1.2.4 快照回归
- `f0faef8` - v1.2.5 场景分桶
- `aada4b0` - v1.2.6 漂移检测

---

## Memory Retrieval Hardening v1.2.8 (2026-03-03 20:24 - 20:28 CST)

### 完成的工作

**v1.2.8**: 操作章程加固
- strict 定义写死：ERROR 永远 fail，WARN 由 strict 决定，WARN_SOFT 永远不 fail
- baseline 来源审计：跨 config_hash 触发 BASELINE_RESET
- 新增字段：`baseline_config_hash`, `config_hash_match`, `baseline_reset`
- 更新 MAINTENANCE.md 操作章程

### 版本完整演进 (v1.2 → v1.2.8)

| 版本 | 核心功能 | 时间 |
|------|----------|------|
| v1.2 | Adoption 指标拆分 + Use 结构化输出 + 语义闸门 + Dashboard | 16:06-18:25 |
| v1.2.1 | 强一致回归测试 + 口径细化 + 风险告警 | 18:32-18:35 |
| v1.2.2 | 样本量门槛 + 分阶段阈值 + orphan 指标 + 自动化观测 | 18:54-18:58 |
| v1.2.3 | 报告版本化 + 可消费摘要 + CI 集成 | 19:04-19:07 |
| v1.2.4 | config_hash 可复现 + orphan 拆分 + 快照回归 + 维护文档 | 19:24-19:35 |
| v1.2.5 | 场景分桶 + linkability 豁免项 | 19:37-19:43 |
| v1.2.6 | 漂移检测：Bucket Mix Drift + Linkability Coverage | 20:00-20:08 |
| v1.2.7 | 热启动期 + 绝对下限 + 解释字段 + 漂移快照回归 | 20:12-20:18 |
| v1.2.8 | 操作章程：strict 定义 + baseline 审计 + config_hash RESET | 20:24-20:28 |

### 工具链

| 工具 | 版本 | 用途 |
|------|------|------|
| `memory-dashboard` | v1.2.8 | 监控面板 + config_hash 显示 |
| `memory-risk-alerts` | v1.2.8 | 风险告警 + baseline 审计 |
| `memory-daily-obs` | v1.2.4 | 每日观测 |
| `ci-memory-hardening` | v1.2.3 | CI 入口 |
| `test_memory_retrieval_hardening.py` | 10 tests | 回归测试 |

### 告警规则（最终版）

| 级别 | strict=true | strict=false | 场景 |
|------|-------------|--------------|------|
| ERROR | fail | fail | broken_link > 5% |
| WARN | fail | 不 fail | unadopted > 10%, 漂移 |
| WARN_SOFT | 不 fail | 不 fail | 热启动期 |
| INFO | 不 fail | 不 fail | 提示信息 |

### 运行命令

```bash
# 每日观测
~/.openclaw/workspace/tools/memory-daily-obs

# CI 检查
~/.openclaw/workspace/tools/ci-memory-hardening

# 回归测试
python3 ~/.openclaw/workspace/tests/test_memory_retrieval_hardening.py
```

### 关键决策

1. **语义闸门**：v1/v2 分离，避免历史脏数据污染
2. **场景分桶**：只对 Bucket A 执行严格告警
3. **漂移检测**：7 日滚动中位数基线
4. **操作章程**：strict 定义写死，config_hash 审计

### 文档位置

- 维护文档：`memory/MAINTENANCE.md`
- Golden fixtures：`memory/golden_fixtures/`
- 历史报告：`memory/reports/daily/`

### Commits

- `aada4b0` - v1.2.6 漂移检测
- `1010e68` - v1.2.7 热启动期
- `0b97cf4` - v1.2.8 操作章程

---

## Memory Retrieval v1.2 修复 (20:40 - 20:43 CST)

### 问题
- `memory-retrieve` Stage 2 未实现，永远返回空结果
- 语义搜索只给 hint，不返回实际文档

### 修复内容
1. 添加 `subprocess` 导入
2. 实现 `stage2_semantic_search()` 调用 OpenViking CLI
3. 移除无效的 `-o json` 参数（OpenViking 默认 JSON）
4. 降低 score 阈值 0.75 → 0.4（适应实际向量相似度分布）
5. 添加 `is_leaf` 过滤（跳过目录节点）

### 验证结果
```bash
# 语义搜索现在返回结果
~/.openclaw/workspace/tools/memory-retrieve "OpenEmotion MVP"
# → 5 docs with score ≥ 0.4

# Pinned 搜索仍正常
~/.openclaw/workspace/tools/memory-retrieve "subagent callback"
# → mem:global:protocol:20260303:subagent
```

### 关键文件
- `tools/memory-retrieve` v1.2 (OpenViking CLI 集成)

---

## 系统状态检查 (20:32 - 20:40 CST)

### ✅ 正常
| 组件 | 状态 |
|------|------|
| Session Index | 139K events, 36K high_signal |
| OpenViking Server | HTTP 8000, embedding 用本机 Ollama |
| subtask-orchestrate | v2.1, pending=0 |
| callback-handler | 可执行 |
| Cron Jobs | 6 个全 ok |
| session-query | FTS5 搜索正常 |

### 长任务执行方案
- **弃用**: Antfarm (未安装)
- **使用**: subagent-run skill + subtask-orchestrate
- 入口: `subtask-orchestrate run "<task>" -m <model> --timeout 300`

---

## 关键词

`memory-retrieve-v1.2` `openviking-cli-integration` `semantic-search-fix` `subagent-orchestrate` `session-archive`

---

**Session End**: 2026-03-03 20:43 CST
## Session Summary (2026-03-03)

### 主要成果
- **LongRunKit 项目完成**: 分布式任务执行系统 (20/20 测试通过)
- **工具实现**: jobctl, jobindex, job-validator, job-pool, job-worker-v3
- **架构**: Distributed lock + MVCC + 可重入 + 幂等性
- **仓库**: https://github.com/pen364692088/LongRunKit

### 技术细节
- Rev mismatch 修复: update_status_safe 替代直接更新
- 测试套件: v0.1 (5), v0.2 (9), P2 (6) 全通过
- E2E 验证: submit → worker → status → result 完整链路

### 关键决策
- 使用 status.json 内嵌 lock 字段 (避免外部依赖)
- Worker 使用 safe 更新避免 rev 冲突
- Indexer 使用 SQLite FTS5 支持全文搜索
- Pool 支持 systemd/cron 两种模式

### 工具位置
- 仓库: ~/Project/Github/MyProject/LongRunKit
- 工具: tools/jobctl, tools/jobindex, tools/job-validator, tools/job-pool
- 测试: tests/test-job-protocol*.py

### 待改进
- [ ] Worker callback 可能需要重试
- [ ] Pool 的健康检查可以更完善
- [ ] 可以添加 metrics/monitoring

---
Updated: 2026-03-03 22:31 CST
