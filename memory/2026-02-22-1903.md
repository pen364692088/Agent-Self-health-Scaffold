# Session Archive — 2026-02-22 19:03 CST

## Summary
- Verified ClawRouter/Gateway/qmd status; fixed gateway service to active running state.
- Installed qmd dependencies via npm global path (`@tobilu/qmd`) after Bun/GitHub route was unreliable in this environment.
- Created Exa MCP skill: `~/.openclaw/skills/exa-mcp/SKILL.md` and validated tool calls with `mcporter`.
- Enabled `memory-lancedb` plugin and confirmed memory slot switched from `memory-core` to `memory-lancedb`.
- Diagnosed VM GPU path: Ollama in VM runs CPU only; confirmed remote host Ollama endpoint reachable and usable.
- Hardened heartbeat prompt and switched heartbeat model to `ollama/qwen2.5:3b-instruct`; validated healthy response path (`HEARTBEAT_OK`).

## Decisions (what changed + why)
- Use host Ollama endpoint (`http://192.168.79.1:11434`) for local-model workloads from VM to avoid VM GPU passthrough limitations.
- Keep daily model split:
  - Main chat model: `opencode/kimi-k2.5-free` (cloud)
  - Heartbeat model: local Ollama (`qwen2.5:3b-instruct`) for zero API cost + better format adherence than 1B.
- Keep heartbeat cadence at 15m for main agent (30m default fallback), balancing detection speed and host load.
- Keep strict heartbeat output policy: healthy => exact `HEARTBEAT_OK`; unhealthy => one-line `ALERT: ...`.

## Open questions / risks
- Small local models can still drift format under some prompts; monitor heartbeat output for a while.
- Current security audit still reports unrelated criticals (permissions/sandbox posture) not addressed in this session.

## Next actions (owner + smallest next step)
- Owner: Manager — monitor next 2-4 heartbeat cycles; verify all healthy cycles emit exact `HEARTBEAT_OK`.
- Owner: User — if any malformed heartbeat appears, send one sample message ID for targeted prompt/guard tuning.
- Owner: Manager — optionally add strict post-filter/whitelist guard if drift reappears.

## Artifacts (paths/links/commands) + keywords
- Skill: `/home/moonlight/.openclaw/skills/exa-mcp/SKILL.md`
- Config: `/home/moonlight/.openclaw/openclaw.json`
- Key commands:
  - `openclaw gateway status`
  - `openclaw plugins enable memory-lancedb`
  - `openclaw status`
  - `qmd --help` / `qmd status` / `qmd search ...`
  - `mcporter call --http-url "https://mcp.exa.ai/mcp?..." --tool web_search_exa --args '{"query":"..."}'`
- Keywords: heartbeat, ollama-host, qwen2.5-3b, memory-lancedb, qmd, exa-mcp, gateway
