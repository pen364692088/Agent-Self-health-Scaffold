# 2026-03-08 Session Memory

## Agent Self-Health Scaffold - Complete Implementation

### Repository
- Location: `/home/moonlight/Project/Github/MyProject/Agent-Self-health-Scaffold`
- Remote: `https://github.com/pen364692088/Agent-Self-health-Scaffold.git`
- Branch: `main`

### Phases Completed

#### P1 (Pre-existing)
- Unified health state output
- Structured incidents
- Basic health checks

#### P2 (Pre-existing)
- Verified recovery with before/after snapshots
- Verdict engine: recovered/unchanged/degraded/insufficient_evidence
- Recovery summary with clean effectiveness metrics
- Evidence refresh actions excluded from recovery rate
- Action governance: whitelist, cooldown, max retry, verification window

#### P3 - Capability Contract & Shadow Proposal
- Capability registry with 7 critical capabilities
- Capability categories: liveness, processing, delivery, memory, tooling, workflow, safety, user_promised_feature
- Verification modes: probe_check, synthetic_input_check, artifact_output_check, recent_success_check, chain_integrity_check
- Forgetting guard: detects stale/degraded/missing capabilities
- Proposal engine with dedup and cooldown
- Level B proposals are proposal-only (never auto-execute)

#### P4 - Main Loop Integration & Gate Wiring
- Scheduler with quick/full modes
- Gate A: Contract integrity validation
- Gate B: E2E capability path verification
- Gate C: Preflight & boundary checks
- Fault injection tests (5 scenarios)
- Instance integration guide

### Key Tools
- `agent-capability-check` - Verify capabilities
- `agent-forgetting-guard` - Detect capability degradation
- `agent-capability-summary` - Capability health report
- `agent-generate-proposal` - Create Level B proposals with dedup
- `agent-proposal-summary` - Proposal flow report
- `agent-self-health-scheduler` - Main loop integration
- `gate-self-health-check` - Gate A/B/C validation

### Critical Decisions
1. Evidence refresh actions (rerun_health_check) are NOT counted as recovery
2. Level B/C actions remain proposal-only forever
3. Gate must pass before any recovery action
4. Capability contracts are machine-readable, not LLM opinions
5. restart_service remains deferred (not opened)

### Security Fix
- Exposed OpenRouter API key in GitHub repo
- Key rotated, removed from:
  - `~/.openclaw/openclaw.json`
  - `~/.config/systemd/user/openclaw-gateway.service`
  - Backup files cleaned

### Test Coverage
- 40 tests passing
- 13 test files
- Fault injection validates real failure scenarios

### Gate Status (Final)
- Gate A: PASS
- Gate B: PASS
- Gate C: PASS

### Next Steps for Instance Owners
1. Read `docs/P4_INSTANCE_INTEGRATION_GUIDE.md`
2. Register instance-specific capabilities
3. Wire into heartbeat cycle
4. Ensure workers expose status signals
5. Monitor summaries and gate reports

## OpenEmotion MVP11.5 progress and guardrails

- User set explicit decision boundary for OpenEmotion roadmap work: stay on MVP11.5, do not move to MVP12, do not relax promotion criteria, and do not switch to Enforced mode until evidence supports it.
- Current verified posture for OpenEmotion is: MVP11.5 still in SHADOW. Engineering wiring is in place, but validation closure is incomplete.
- Key readiness language was tightened during the session:
  - Allowed: `wiring_ready = PASS`, `shadow_ready = PASS`.
  - Not yet allowed: `natural_ready`, promotion-criteria satisfaction, long-run stability, or real-world distribution readiness.
- T06 originally overclaimed runtime readiness; this was formally corrected. T06 is only partially complete in the original sense because runtime integration was initially missing.
- T06.5 was introduced and completed as a minimal runtime integration task:
  - `emotiond/models.py` gained `intent_contract` on `PlanResponse`.
  - `emotiond/core.py` now generates `intent_contract` in `generate_plan()`.
  - `emotiond/core.py` now runs checker logic for `assistant_reply` events in shadow mode before returning result data.
  - Runtime integration was validated via direct import / main path execution, not just testbot scenarios.
- T07 controlled runtime-path rerun established evidence layering discipline:
  - Layer 1 = test data only.
  - Layer 2 = controlled runtime-path data with non-empty, non-`test_*` session IDs.
  - Layer 3 = natural runtime data; currently none.
- T07 result: controlled runtime-path evidence proved wiring and shadow logging, but did not prove natural readiness. Recommendation remained to stay in shadow.
- T07.1 expanded Layer 2 controlled runtime-path soak to 50 samples and found:
  - violation_rate around 34% on Layer 2 sample set,
  - top violation class is `fabricated_numeric_state`,
  - safe-response false positives were effectively 0,
  - checker false negatives for `epistemic_upgrade` and `commitment_upgrade` remained effectively 100%.
- User decision after T07.1: do not pivot to Layer 3 yet. Instead run T07.2 Checker Hardening + Contract Tightening first, because certainty/commitment false negatives would distort any natural-runtime readiness judgment.
- T07.2 requirements from user:
  - strengthen checker for certainty and commitment upgrades, especially Chinese high-frequency patterns,
  - tighten response intent contract / forbidden claims for interpreted mode,
  - add regression scenarios focused on certainty/commitment upgrades,
  - rerun Layer 2 after hardening,
  - still remain in MVP11.5 + SHADOW.

## Operational preference learned

- When implementation gets stuck on subtle code-path / wiring / integration reasoning, user explicitly wants temporary escalation to `openai-codex/gpt-5.4`, solve the issue there, then switch back and continue.

---

## T07.2 Checker Hardening + Contract Tightening - COMPLETE

### Root causes fixed
1. **Runtime path using wrong checker**: Was calling `self_report_consistency_checker` instead of `response_intent_checker`
2. **Wrong contract shape**: Runtime was feeding `report_policy` shaped contract instead of `intent_policy` shaped
3. **Missing import**: `check_intent` was referenced but not imported in `emotiond/core.py`
4. **Pattern blind spots**: Chinese certainty/commitment patterns were too sparse

### Files modified
- `emotiond/self_report_interpreter.py` - added `interpret_to_intent_contract()` for runtime contract shape
- `emotiond/core.py` - rewired to `response_intent_checker`, fixed import
- `emotiond/response_intent_checker.py` - expanded certainty/commitment patterns, fixed forbidden-internalization slice logic
- `tests/test_response_intent_checker.py` - added Chinese hardening regression tests
- 4 new scenario files in `tests/testbot/scenarios/t07_2_*.json`

### Results
- epistemic_upgrade detection recall: 0% → 100% (4/4)
- commitment_upgrade detection recall: 0% → 100% (4/4)
- safe_control false positive: 0/2
- Commit: `3c6b77c`

---

## T07.3 Mixed Layer 2 Stabilization Rerun - COMPLETE

### Sample design
- sample_size: 100 (all Layer 2 controlled runtime-path)
- Mixed distribution: numeric_fabrication(20), qualitative_fabrication(20), certainty_upgrade(15), commitment_upgrade(15), multi_turn_drift(10), safe_controls(15), edge_cases(5)

### Results
- overall_violation_rate: 63.0%
- Top violation classes: commitment_upgrade(47), certainty_upgrade(45), numeric_leak(36), tone_escalation(2)
- safe_control false positive: 0/15
- Commit: `d8217a5`

### Key findings
1. certainty/commitment now observable in main Layer 2 statistics (no longer blind)
2. numeric_leak still a major class but no longer dominates
3. safe_control false positives remain low
4. **shadow_log.jsonl not receiving entries** - runtime checker works, but shadow-log persistence incomplete

### Comparability notes
- T07.1 vs T07.3: directionally comparable (both Layer 2 controlled runtime-path)
- T07.2 vs T07.3: not comparable for overall violation_rate (targeted vs mixed)
- T01/T04 vs T07.3: not comparable (Layer 1 vs Layer 2)

### Recommendation
Continue in SHADOW. Layer 3 natural evidence still missing. Promotion criteria unmet.

---

## Context Compression Pipeline Status (discovered at session end)

- **Current state**: S1 Shadow Validation (Feature Frozen)
- **Enabled**: ❌ Not integrated into main chain
- **Gate 0**: ✅ PASSED
- **Gate 1**: ⏳ PENDING (sample 12/80)
- **Result**: Context reached 221k/200k (111%) but automatic compression did not trigger
- **Reason**: Compression tools exist but Gate 1 not passed, so not integrated into session management

### Available tools (manual use only)
- `tools/context-budget-check`
- `tools/context-compress`
- `tools/context-retrieve`

---

## Active Blockers for OpenEmotion

1. `NO_NATURAL_RUNTIME_EVIDENCE` - Layer 3 data still missing
2. `PROMOTION_CRITERIA_UNMET` - violation_rate still high
3. `SHADOW_LOG_PERSISTENCE_INCOMPLETE` - runtime checker returns results but doesn't write to shadow_log.jsonl

---

## User Operational Preferences (reinforced)

- When stuck on implementation details, switch to `openai-codex/gpt-5.4` temporarily, then switch back
- Strict data layering: never mix Layer 1/2/3 for readiness claims
- Stay in MVP11.5, do not enter MVP12, do not relax promotion criteria, stay in SHADOW mode


## 2026-03-08 20:22 CST — Native compaction safeguard bugfix investigation status

- User-directed task focused on fixing native compaction safeguard false-positive: `Compaction safeguard: cancelling compaction with no real conversation messages to summarize.`
- Confirmed original root cause hypothesis: safeguard predicates were reading top-level `.role/.content` while real session entries commonly store message data under `.message.role/.message.content`.
- Located live compaction warning sites across OpenClaw dist bundles, especially:
  - `dist/reply-C5LKjXcC.js`
  - `dist/compact-B247y5Qt.js`
  - `dist/pi-embedded-*.js`
  - `dist/plugin-sdk/dispatch-*.js`
  - `dist/plugin-sdk/reply-DbZnH8-h.js`
- Important finding: earlier patch attempt became a **half-patch** in several runtime bundles: alias variables like `const msg = message.message || message;` were inserted, but return predicates still referenced old fields like `message.role` or mixed `msg.role` / `m.role`.
- Confirmed live gateway runtime imports `reply-C5LKjXcC.js` directly from `index.js`; most credible live safeguard call path is:
  - `index.js`
  - -> `reply-C5LKjXcC.js`
  - -> `compactionSafeguardExtension(api)`
  - -> `api.on('session_before_compact', ...)`
  - -> `isRealConversationMessage(...)`
- Completed full rewrite of those half-patched predicates in live/runtime-adjacent bundles so predicate bodies consistently use nested-or-flat aliases.
- Restarted gateway successfully and confirmed new PID/start time during investigation.
- Despite restart + full-patch rewrite, **final live revalidation still failed**.
- Critical evidence: under real live session pressure at **174k/200k (87%)**, the false-positive warning recurred:
  - `2026-03-08T20:18:47.533-05:00 [compaction-safeguard] Compaction safeguard: cancelling compaction with no real conversation messages to summarize.`
- Therefore the durable final status is:
  - **Bugfix revalidation: FAILED**
  - **Bug cannot be closed**
  - **Phase D: not evaluated / must not be advanced**
- What has now been ruled out:
  - not merely “failed to reach live threshold”
  - not merely “gateway restart did not apply”
- Best current root-cause scope is narrowed to three classes:
  1. still-unpatched real runtime path
  2. actual live entry structure is more complex than `.message.role`
  3. candidate message list is already filtered empty before safeguard final check
- Agreed next-step strategy with user: stop blind bundle patching and stop threshold experiments; next task should be **runtime instrumentation / trace capture** at the live safeguard decision point.
- Instrumentation must capture at least:
  - actual live file/function hit
  - candidate set size before safeguard decision
  - sample entry structure summaries (first 3–5)
  - top-level keys per entry
  - presence of `entry.role`, `entry.message`, `entry.message.role`, `entry.message.message.role`
  - per-entry reason codes for real/not-real classification
  - whether upstream filtering already emptied candidate set
- Key wording agreed with user for state framing:
  - current bug has escalated from a “half-patch problem” to a “live safeguard runtime still has an unexplained path or uncovered data shape”; next step must be runtime instrumentation before choosing the minimal repair.

## 2026-03-08 21:15 CST — Session wrap-up

### Key topics
- Root-caused native compaction safeguard failures from symptom-level misread to true upstream compaction-preparation failure.
- Proved live false-positive was not primarily safeguard role parsing after runtime instrumentation showed `messagesToSummarize=[]` and `turnPrefixMessages=[]` before safeguard logic.
- Pinned live outer path and internal compaction entry chain in OpenClaw runtime.
- Identified single-point semantic origin in `@mariozechner/pi-coding-agent/dist/core/compaction/compaction.js`, function `prepareCompaction(pathEntries, settings)`.

### Decisions made
- Freeze status remains:
  - Config Alignment Gate: PASS
  - Phase C / Controlled Validation: PASS
  - Phase D / Natural Validation: BLOCKED
  - Safeguard bugfix revalidation: FAILED
  - Native compaction safeguard bug: OPEN
- Do not keep patching safeguard predicates as the primary fix path.
- Treat the observed double-empty preparation as a compaction-builder problem, not a safeguard entry-shape problem.
- Choose root-fix strategy **Option B (hotfix)** for now:
  - detect degenerate empty preparation in `prepareCompaction(...)`
  - throw precise upstream reason `invalid_cut_point_empty_preparation`
  - keep safeguard as last insurance layer, not root-cause classifier.

### What worked
- Runtime instrumentation at `session_before_compact` successfully proved the hook was receiving an empty preparation candidate set.
- Upstream tracing proved outer wrapper stages stayed non-empty.
- Internal narrowing pinned real implementation flow to:
  - `reply-C5LKjXcC.js:5263` `async compact(params)`
  - `reply-C5LKjXcC.js:74319` `compactEmbeddedPiSessionDirect(params)`
  - handoff around `reply-C5LKjXcC.js:74713`
- Unminified source discovery in bundled dependency confirmed exact single-point root cause in `prepareCompaction(...)`.

### Problems solved / clarified
- Eliminated wrong root-cause hypotheses:
  - not merely threshold not reached
  - not merely gateway not restarted
  - not primarily safeguard field misread
- Clarified stronger root-cause class:
  - `conditional_skip_not_loaded`
- Clarified likely cut-point degeneracy:
  - `firstKeptEntryIndex === boundaryStart`
  - `isSplitTurn === false`
  - real history exists, yet both preparation arrays remain default-empty.

### Code / patch state
- Added root-fix hot patch in:
  - `.../pi-coding-agent/dist/core/compaction/compaction.js`
  - `.../pi-coding-agent/dist/core/agent-session.js`
- Added deterministic regression coverage artifact:
  - `artifacts/compaction_safeguard_bugfix/tests/prepare_compaction_regression_test.mjs`
- Test classes covered:
  - live-like regression
  - valid non-split path
  - valid split-turn path
  - truly empty history

### Deliverables produced
- `ROOT_FIX_DESIGN.md`
- `CUTPOINT_DEGENERATE_CASES.md`
- `PREPARE_COMPACTION_REGRESSION_TESTS.md`
- `ROOT_CAUSE_SINGLE_POINT.md`
- `PREPARATION_SOURCE_MAP.md`
- `EMPTY_BRANCH_DECISION_TABLE.md`
- plus full upstream tracing / callpath / runtime evidence files in `artifacts/compaction_safeguard_bugfix/`

### Gotchas / lessons
- Bundle-level patching can become a half-patch; semantic origin may live in vendored unminified dependency code, not only top-level runtime bundles.
- When safeguard sees empty preparation arrays, the real bug may be upstream candidate construction, not final message classification.
- For this class of bug, first-write/source tracing is more valuable than another round of downstream predicate patching.

### Follow-up next session
- Sync the root fix into the actual live runtime path if still needed after dependency/source path alignment.
- Re-run live native compaction revalidation against the root-fixed preparation path.
- Confirm whether live compaction now yields:
  - no false-positive
  - compactions 0 -> >0
  - context ratio drop
- Keep Phase D blocked until true natural live evidence exists.


## Compaction safeguard / live bundle adoption investigation

- Verified earlier verdict `root_fix_not_adopted`: live runtime was executing packed bundle `~/.npm-global/lib/node_modules/openclaw/dist/reply-C5LKjXcC.js`, while standalone nested dist file `node_modules/@mariozechner/pi-coding-agent/dist/core/compaction/compaction.js` already contained the root-fix.
- Confirmed bundle/source split: live bug was not primarily cache illusion; the loaded packed artifact itself lacked `InvalidCutPointEmptyPreparationError` and `invalid_cut_point_empty_preparation` before redeploy.
- Mapped build/redeploy chain at high level: upstream OpenClaw bundle comes from `pnpm build` -> `node scripts/tsdown-build.mjs` -> `tsdown.config.ts`; installed npm artifact is trimmed and does not ship local source/build scripts, so in-place rebuild was not available inside the installed package.
- Adopted minimal live fix by patching the actual loaded bundle `reply-C5LKjXcC.js` directly, backing it up first, restarting `openclaw gateway`, and verifying new bundle hash.
- Post-adoption proof: live bundle now contains `InvalidCutPointEmptyPreparationError`, `invalid_cut_point_empty_preparation`, and `session_before_compact_invalid_cut_point_empty_preparation`.
- Current post-adoption verdict: `second_root_cause_suspected_after_adoption` — bundle adoption succeeded, but minimal live revalidation did not yet produce a fresh compaction semantic event or functional restoration.
- Observed runtime after adoption: live direct session `agent:main:telegram:direct:8420019401` still shows `Compactions: 0`; repeated `/compact` injections via `sessions_send` time out waiting for completion.
- Strongest new runtime clue is session-lane blockage rather than bundle non-adoption: gateway logs show `lane wait exceeded: lane=session:agent:main:telegram:direct:8420019401 waitedMs=129806 queueAhead=0`.
- Repeating `/compact` is not useful right now; likely next step is to identify what is holding the session lane for `agent:main:telegram:direct:8420019401` and why queued compaction work is not draining.
- Artifacts written under `artifacts/compaction_safeguard_bugfix/` and `artifacts/compaction_safeguard_bundle_adoption/`; latest commit created in workspace: `488735d` (`Document live bundle adoption and post-adoption revalidation`).
